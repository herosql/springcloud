spring:
  application:
    name: uaa-service
  cloud:
        config:
          discovery:
            enabled: true #开启通过服务来访问Config Server的功能
            service-id: config-server #在eureka中注册的服务名
  profiles:
      active: pro
